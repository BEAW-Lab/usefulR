% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_ESAWC21_raster.R
\name{download_ESAWC21_raster}
\alias{download_ESAWC21_raster}
\title{Download ESA WorldCover 2021 data}
\usage{
download_ESAWC21_raster(
  local_dir = NULL,
  file_name = "LC_raster.tif",
  force = FALSE
)
}
\arguments{
\item{local_dir}{Path to directory where the file should be saved.
If NULL (default), a system cache directory is used.}

\item{file_name}{Desired name for the downloaded file (e.g. "LC_raster.tif").
Must include the ".tif" extension. Defaults to "LC_raster.tif".}

\item{force}{Logical. If TRUE, re-downloads even if the file already exists.}
}
\value{
The full path to the downloaded raster file.
}
\description{
This helper function downloads a land cover raster from ESA WorldCover 2021.
It stores it locally, so it needs to be downloaded only once.
}
\examples{
\dontrun{
# Default: saves in cache with default name
path <- download_LC_raster()

# Save in custom folder
path <- download_LC_raster(local_dir = "data_raw/")

# Save with custom name
path <- download_LC_raster(local_dir = "data_raw/", file_name = "Spain_LC_2021.tif")
}
}
